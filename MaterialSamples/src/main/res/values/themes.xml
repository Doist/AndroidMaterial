<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material.Light">
        <item name="colorButtonNormal">@color/btn_sample</item>
    </style>

    <!-- Sample overlay theme. -->
    <style name="OverlayTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@android:color/holo_blue_light</item>
        <item name="editTextColor">?attr/colorAccent</item>
    </style>

</resources>
